import createStyles, {colors} from '../../style/base';

const trainingStyles = createStyles({
  containerStyle: {
    flex: 1,
    backgroundColor: colors.backgroundColor,
  },
});

export default trainingStyles;
