import createStyles from '../../style/base';

const languageSelectorStyles = createStyles({
  containerStyle: {flex: 1, justifyContent: 'flex-end', flexDirection: 'row'},
  pickerStyle: {height: 50, width: 90},
});

export default languageSelectorStyles;
