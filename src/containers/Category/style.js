import createStyles, {colors} from '../../style/base';

const categoryStyles = createStyles({
  containerStyle: {
    flex: 1,
    backgroundColor: colors.backgroundColor,
  },
});

export default categoryStyles;
