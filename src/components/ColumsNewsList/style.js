import createStyles, {colors} from '../../style/base';

const columnsNewsListStyles = createStyles({
  containerStyle: {
    flex: 1,
    backgroundColor: colors.backgroundColor,
  },
});

export default columnsNewsListStyles;
